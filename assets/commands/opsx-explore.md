---
description: 探索 OpenSpec 变更思路
agent: build
---

围绕 `$ARGUMENTS` 做需求澄清、范围收敛和实现思路探索。这是一个思考阶段，不是实现阶段。

**探索模式 = 思考，不实现。** 可以读文件、搜索代码、研究代码库，但绝不能写代码或实现功能。

如果用户有模糊想法、具体问题、或需要对比方案时很有用。

建议输出：
1. 要解决的问题
2. 范围与非目标
3. 风险与依赖
4. 下一步建议使用 `/opsx-propose` 启动变更

不要实现任何功能或修改代码——这是纯粹的探索阶段。