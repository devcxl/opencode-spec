---
description: 更新 OpenSpec design
agent: build
---

为变更 `$ARGUMENTS` 生成或更新设计文档。

先调用工具 `openspec-design`：
- name: $ARGUMENTS

然后：
1. 阅读该 change 的 `proposal.md`
2. 补全目标、约束、技术方案、影响范围、风险与缓解措施
3. 确保设计与 proposal 保持一致
